# Local Ollama Configuration
LLM_BASE_URL=http://localhost:11434/v1
LLM_API_KEY=ollama

# Recommended from your list: Command R (Great for RAG/Analysis)
LLM_MODEL=command-r:latest

# Optional: Smart LLM for high-level synthesis & taxonomy design (e.g. GPT-4o, Claude 3.5, or large local model)
# If not set, defaults to LLM_MODEL
SMART_LLM_MODEL=

# Your specified embedding model
EMBEDDING_MODEL=qwen3-embedding:0.6b

# Database Configuration
DB_HOST=localhost
DB_NAME=hypertension_db
DB_USER=postgres
DB_PASSWORD=password
DB_PORT=5432
RESEARCH_TOPIC=Hypertension and High Blood Pressure